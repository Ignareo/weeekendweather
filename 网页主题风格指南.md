# 网页主题风格指南

> 来源项目：「周末去哪 · 天气筛选」。四套主题在同一套 HTML 骨架上通过 CSS 变量 + 主题注册表切换，本文档整理每套风格的完整规格（色彩 / 字体字形 / 造型 / 图标 / 插画 / 图表配色），可直接复用到其他网页。

## 〇、主题复用机制

每套主题由四要素构成，新增主题时按此四步接入：

1. **CSS 变量**：以 `body[data-theme="xxx"] { --bg: ...; --surface: ...; }` 覆盖语义变量（bg / surface / ink / primary / ok / warn / err / radius / shadow / serif / body-font），组件全部只引用变量、不写死颜色。
2. **字体**：Google Fonts 引入拉丁字体；中文大字体用 `pyftsubset` 抽取页面实际用到的字符做 woff2 子集（约 20KB），`@font-face` 挂载。
3. **图标函数**：注册 `xxxIcon(tier, size)`，输入雨况档位 0–3，返回内联 SVG 字符串。
4. **主题注册表**：
   ```js
   THEME_DEFS = {
     xxx: { img: "hero-xxx.png",      // 顶部插画
            icons: xxxIcon,           // 图标函数
            palette: [8 色图表色板] }, // 图表/图例配色
   }
   ```
   另需在防闪烁早期脚本的白名单中加入主题 key，并用 localStorage 持久化用户选择。

**雨况档位语义（四主题共用）**：`0 = 无雨/晴`、`1 = 小雨`、`2 = 中雨`、`3 = 大到暴雨/雷`。

---

## 一、浮世绘 ukiyo（默认主题）

**关键词**：葛饰北斋《神奈川冲浪里》、米色和纸、侘寂、纤细金线、暗色模式。

### 色彩

| 角色 | 色值 | 说明 |
|---|---|---|
| 页面底色 | `#f4efe3` | 米色和纸 |
| 卡片 | `#fdfbf3` / `#f4efe0` | 浮起的白纸 |
| 边框 | `#e4dac2` | 1px 实线 + 点状分隔线 |
| 文字 | `#3b3324` / `#7d7059` / `#ac9f85` | 墨褐三级 |
| 主色 | `#8a6a33`（hover `#6f5426`） | 古金棕 |
| 点缀金 | `#c99a3f` | 分隔线、图标 |
| 成功/警告/错误 | `#5e8a49` / `#a56f2b` / `#b04a38` | 低饱和 |
| 雨色 | `#8fb3cf`（小雨）/ `#5b8cb5`（中雨） | 灰蓝 |

暗色模式（仅本主题跟随系统）：底 `#171410`、卡 `#221d14`、墨 `#eae1cc`、主色 `#cfa55c`。

### 字体与字形

- 标题：`"Noto Serif SC", "Songti SC", serif`，字重 500，**超大字距**（主标题 letter-spacing 14px，节标题 2.5px）
- 正文：系统黑体栈 `-apple-system, "PingFang SC", "Microsoft YaHei", sans-serif`
- 处理：标题下加 52px 细金线（1px border-bottom），竖排感留白

### 造型语言

- 圆角：大卡 20–26px，小控件 10px，按钮全圆角 999px
- 阴影：极柔和的褐色投影 `0 12px 32px rgba(84,68,38,.08)`
- 分隔：1px 点线（dotted）代替实线
- 按钮：白底细边，选中态古金棕填充

### 图标（线性风）

- 晴＝金色太阳（`#d2a13f` 圆 + 8 条 1.6px 射线）
- 雨＝雨伞剪影（小雨 `#8fb3cf`、中雨 `#5b8cb5`）
- 大到暴雨＝金色闪电 `#dfa63f`
- 统一 `viewBox 24×24`，默认渲染 14px，无描边填充式

### 插画与图表

- 插画：透明底浮世绘海浪 PNG（wave.png），深色模式不反色
- 图表色板：`#2f6f9f #c0503a #5e8c3f #c99a3f #7d5ba6 #2a8f85 #c4647f #6e5849`（ muted 复古八色）

**适用**：文化、阅读、茶/器/手工艺、安静正式的内容站。**注意**：金色 `#c99a3f` 不用于正文文字，只做线与小图标。

---

## 二、像素 pixel

**关键词**：8-bit 红白机、硬阴影、低分辨率网格、按压反馈。

### 色彩

| 角色 | 色值 | 说明 |
|---|---|---|
| 页面底色 | `#cfe3ee` | 浅蓝屏底色 |
| 卡片 | `#fffdf2` / `#f3edd9` | 米白 |
| 边框 = 文字 | `#23273a` | 深海军蓝，**边框即墨色** |
| 次级文字 | `#4a5168` / `#8a8fa3` | |
| 主色 | `#e0506e`（hover `#c93a58`） | 像素红粉 |
| 点缀黄 | `#f6bd2f` | 太阳、高亮 |
| 成功/警告/错误 | `#2a9d5f` / `#d97b29` / `#d43d3d` | 高饱和纯色 |
| 雨色 | `#6aa6d8` / `#4a90d9` | FC 蓝 |

### 字体与字形

- 拉丁/数字：`"Press Start 2P", monospace`（Google Fonts）
- 中文：`ZpixSub`（Zpix 最像素，按页面用字做的 19KB woff2 子集，`@font-face` 挂载）
- **全局字号压到 12px**，标题 letter-spacing 8px；像素字体不换行不抗锯齿，`image-rendering: pixelated` 作用于插画

### 造型语言

- 圆角：卡 4px、控件 2px（近似直角）
- **硬偏移阴影**：卡 `5px 5px 0 rgba(35,39,58,.25)`，按钮 `3px 3px 0 当前墨色`
- 边框：一律 2px 实线 `#23273a`
- 交互：hover 下移 1px、active 下移 3px 且阴影消失（模拟按键）
- 滑块手柄等圆点一律改方形

### 图标（字符网格像素画）

用字符画网格渲染：12×12 字符数组，每格 2×2 SVG rect，`shape-rendering: crispEdges`。

- 晴＝`#f6bd2f` 太阳；云＝`#9fb3c8`；雨滴＝`#4a90d9`（小雨 2 列、中雨 3 列）；雷＝云 + 黄色折线闪电

### 插画与图表

- 插画：像素海浪 + 橙色太阳 PNG，放大显示时必须 `image-rendering: pixelated`
- 图表色板：`#e0506e #3a86ff #ff9f1c #8338ec #06a77d #e9c46a #9c6644 #f72585`（高饱和八色）
- 图表字体随主题切 Press Start 2P，图例圆点保持无阴影

**适用**：独立游戏、复古工具、极客向产品。**注意**：中文必须用子集像素字体，全字库 Zpix 体积过大；字号低于 12px 可读性骤降。

---

## 三、中国风 cn

**关键词**：宣纸、水墨山水、朱砂印章、行书标题。

### 色彩

| 角色 | 色值 | 说明 |
|---|---|---|
| 页面底色 | `#ece5cf` | 宣纸黄 |
| 卡片 | `#f8f3e2` / `#efe7cd` | 浅色宣纸 |
| 边框 | `#cfc19c` | 草木染灰金 |
| 文字 | `#2e2b25` / `#6e6450` / `#a0927a` | 烟墨三级 |
| 主色 | `#b3452e`（hover `#93331f`） | **朱砂** |
| 成功/警告/错误 | `#5b6e4e` / `#a8702a` / `#a63a2b` | 松柏绿 / 赭石 / 绛红 |
| 雨色 | `#8ba3b8` / `#5e7d99` | 黛蓝 |

### 字体与字形

- 标题：`"Ma Shan Zheng", "Noto Serif SC", serif`（马善政毛笔行书，Google Fonts），主标题 42px、字距 8px、字重 400
- 正文：`"Noto Serif SC", "Songti SC", serif` 全宋体栈
- 印章内文字用宋体 11px

### 造型语言

- 圆角：卡 8px、控件 4px（比浮世绘更克制）
- 阴影：近无 `0 10px 28px rgba(60,48,28,.09)`
- 无花哨交互，hover 仅变色

### 图标（朱砂印章）

24×24 SVG：`rect rx=4` 印章框 + 居中汉字，四种白文/朱文变化：

- 晴＝朱砂描边空印「晴」（outline）
- 小雨＝朱砂 16% 淡底「雨」（light）
- 中雨＝朱砂实底米白字「雨」（full）
- 大到暴雨＝墨色 `#3a352c` 实底「雷」（dark）

### 插画与图表

- 插画：水墨远山 + 朱砂小印章 PNG（wave-cn.png），宽度可比默认放大至 66vw
- 图表色板：`#2e2b25 #b3452e #3f6b4f #b98a2e #3d5a80 #7a4a56 #2f6f6f #c2611f`（墨、朱砂、松绿、藤黄、黛蓝、绛紫、苍绿、赭橙）

**适用**：文博、茶饮、节气、传统节日活动页。**注意**：行书字体只用于标题，正文长段落用宋体；朱砂大面积使用会刺眼，只做点睛。

---

## 四、多巴胺 dopa

**关键词**：starface.world 复刻、黑色画布、电光黄、贴纸拼贴、胖圆字、Y2K。

### 色彩

| 角色 | 色值 | 说明 |
|---|---|---|
| 页面画布 | `#111111` | **近黑底，卡片缝隙露黑** |
| 顶部大卡 | `#fdff00` | 电光黄（品牌色） |
| 普通卡片 | `#ffffff` / `#dcf8ff` | 白 / 冰蓝 |
| 图表卡 | `#000000` | 纯黑 + 白字 |
| 文字（卡上） | `#111` / `#4d4d4d` / `#8a8a8a` | |
| 主色 = 点缀 | `#fdff00` | 选中态、slider 填充 |
| hover 色 | `#ff9cee` | 泡泡糖粉 |
| 强调 | `#ff07da` | 热品红（✦ 装饰、focus 环） |
| 成功/警告/错误 | `#26ba28` / `#ff6b35` / `#ff07da` | 霓虹纯色 |
| 雨色 | `#00cced` / `#00adef` | 电光青 |

### 字体与字形

- 中文标题：`"ZCOOL KuaiLe"`（站酷快乐体，Google Fonts，免费可商用），胖圆活泼
- 拉丁/数字：`"Fredoka"` 600/700（最接近 ABC Diatype Rounded 的免费圆胖体）
- 正文：系统黑体栈（保证可读）
- 处理：英文一律**小写**；主标题 `transform: rotate(-1.5deg)` 微倾斜；日期/副标用 Fredoka

### 造型语言

- 圆角：大卡 22px，**按钮/输入框全胶囊 999px**
- **零阴影**，靠黑画布缝隙分隔卡片
- 边框：按钮/输入 2px 纯黑 `#111`
- 按钮三态：常态白底黑字 → hover 泡泡粉 → 选中电光黄
- 徽章：黑色小方块/胶囊 + 电光黄粗字（复刻 "new" 徽章）
- 表头：黑底 + 电光黄 Fredoka 字
- 节标题前缀：品红四角星 `✦`（不用线）
- Toast：黄底黑字黑边

### 图标（贴纸风）

粗黑描边（1.8–2.2px）+ 霓虹填充 + **笑脸**（两眼 + 弧线嘴）：

- 晴＝`#fdff00` 太阳 + 2.1px 黑射线 + 笑脸
- 雨＝`#dcf8ff` 云（黑描边 + 笑脸）+ 彩色雨滴：黄 `#fdff00` / 品红 `#ff07da` / 青 `#00cced` 轮换（小雨 2 滴、中雨 3 滴），雨滴为 2.2px 圆头斜线
- 大到暴雨＝黄闪电 + 1.7px 黑描边

### 插画与图表

- 插画：透明底贴纸拼贴 PNG（wave-dopa.png）——四色浪花（品红/青/黄/绿）+ 笑脸太阳 + 四角星，白贴纸描边
- 图表：黑底卡 + 霓虹八色 `#fdff00 #ff07da #00cced #26ba28 #8d3eff #ff9cee #ff6b35 #5cddf7`，坐标文字半透明白，tooltip 黄底黑字圆角 12px
- 图例圆点透明底（需防全局按钮样式污染，单独 reset）

**适用**：快消、美妆、潮玩、活动营销页。**注意**：电光黄 `#fdff00` 绝不用于白底上的文字/细线（只配黑底或黑字）；AI 生成插画需检查并清除半透明水印；本主题不跟随系统暗色模式，画布固定黑。

---

## 五、四主题速查对比

| 维度 | 浮世绘 | 像素 | 中国风 | 多巴胺 |
|---|---|---|---|---|
| 底色 | 米纸 `#f4efe3` | 浅蓝 `#cfe3ee` | 宣纸 `#ece5cf` | 黑 `#111111` |
| 主色 | 古金棕 `#8a6a33` | 像素红 `#e0506e` | 朱砂 `#b3452e` | 电光黄 `#fdff00` |
| 标题字体 | Noto Serif SC | Zpix + Press Start 2P | Ma Shan Zheng | ZCOOL KuaiLe + Fredoka |
| 字形处理 | 大字距 + 细金线 | 12px 小字号 | 行书大字 | 全小写 + 微倾斜 |
| 圆角 | 20px | 4px | 8px | 22px / 胶囊 |
| 阴影 | 柔和褐影 | 硬偏移投影 | 近无 | 无（黑缝分隔） |
| 图标语言 | 线性金/蓝 | 字符网格像素画 | 朱砂印章汉字 | 粗黑边贴纸 + 笑脸 |
| 图表色 | 复古 muted | 高饱和纯色 | 国画矿物色 | 霓虹荧光 |

## 六、通用注意事项

1. **组件不写死颜色**，全部走语义变量，新增主题只需覆盖变量 + 注册表。
2. 主题切换后立即重渲染表格/图表（图表的 `Chart.defaults` 也要随主题设置文字、网格、字体、tooltip 色）。
3. 图例、tab 等"伪装成普通元素"的按钮，在新主题里要单独 reset 背景/边框，防止被全局 button 样式污染。
4. 中文网络字体务必子集化；切换主题不刷新页面，字体用 `display=swap` 避免阻塞。
5. 每套主题的图表色板需满足：相邻色相间隔 ≥ 40°，且在各自图表卡底色上都清晰可辨。
