# 网页版实施规划

目标：将现有命令行工具升级为**纯前端静态网页**，部署到 GitHub Pages，浏览器直接完成"抓预报 → 筛选 → 决策汇总"全流程。Python 脚本保留，仅用于本地刷新站点缓存与离线使用。

## 已验证的技术前提（2026-08-11 实测）

| 结论 | 影响 |
|------|------|
| `/rest/weather`（7 天预报）返回 `Access-Control-Allow-Origin: *` | 浏览器可直连，核心功能无需后端 |
| `/rest/findAlarm`（气象预警）同样带 CORS `*` | 预警功能可纯前端实现 |
| `/f/rest/*`（站点列表、tempchart）**无 CORS 头** | 站点代码必须内置 `stations.json` |
| `/rest/weather` 单次响应**自带** `data.tempchart`（前 7 天实况 + 后 7 天预报） | 14 天温度走势纯前端即可画，无需 Actions 兜底 |
| `/rest/weather` 自带 `data.real.weather.feelst`（体感温度）、湿度、`rcomfort/icomfort`（舒适度指数） | 体感温度零成本接入 |
| `/rest/weather` 自带 `data.air`（AQI 数值 + 等级文字） | 空气质量零成本接入 |
| `/rest/weather` 自带 `data.real.sunriseSunset`（日出日落）、`data.real.warn`（实时预警） | 可选增强字段 |
| 响应含两个发布时间：`data.predict.publish_time`（预报发报）、`data.real.publish_time`（实况） | 页面数据源时间显示的依据 |
| `stations.json` 212KB / 2522 站 / 34 省 | 直接作为静态资源部署，gzip 后约几十 KB，可接受 |
| 预警 `alertid` 前缀是行政区划代码，`stations.json` 无此字段 | 按省查预警列表，再用城市名匹配 `title` 字段 |

风险：以上均为 NMC 未公开接口，CORS 策略与接口结构随时可能变化。网页需做好请求失败的降级与报错。

## 目标架构

```
index.html          单页应用（抓取 + 筛选 + 汇总 + 图表 + 变更对比）
stations.json       站点代码缓存（已存在，直接复用）
holidays.json       法定节假日数据（新增，每年更新一次）
data.json           Actions 定时抓取的天气快照（可选兜底）
Python 脚本/CSV     保留不动，用于本地 refresh 与离线使用
```

- 页面打开时并发请求各城市 `/rest/weather`（当前 24 城约 24 个请求，秒级完成；一次响应同时拿到预报、实况、体感、AQI、tempchart）
- 用户选择（城市列表、筛选条件、出行元数据）与**上次抓取快照**存 `localStorage`
- 分享状态编码进 URL hash，无需服务端
- 零构建：纯 HTML/JS，图表库走 CDN

---

## 阶段一：核心移植（MVP）

把 `nmc_weather.py` + `filter_weather.py` 的能力搬进浏览器。

**内容**
- 单页 `index.html`：加载内置 `stations.json` 与默认城市列表（取自当前 `cities.txt`），并发抓取 `/rest/weather`
- 移植筛选逻辑：日期范围、雨天接受度（四档）、白天温度上限、夜间温度下限、至少几天达标；雨况档位判定规则（文字 + 降水量取较严重者）原样照搬
- 输出与 CLI 一致的"每城市一行"汇总表（达标天数、逐日雨况图标、综合雨况、温度区间、总降水），同样排序规则
- **数据源时间显示（需求 2）**：每城市行显示预报发报时间（`predict.publish_time`，精确到城市）；页面顶部显示本次页面抓取时刻。若某城市发报时间明显滞后于其他城市（如差一个发报批次）给出提示
- 顺带展示同响应自带的免费字段：体感温度、湿度、AQI（等级文字）——不加列，收进城市行的展开详情里
- **站点识别失败处理（需求 6）**：城市在内置 `stations.json` 中查不到时，页面显著报错："站点代码未收录/已失效，请本地运行 `python3 nmc_weather.py refresh` 后将新 `stations.json` 提交部署"；该城市标红但不阻塞其他城市
- NMC 接口请求失败（网络异常、结构变更）时给出明确错误提示

**验收**：Pages 部署后打开页面即显示当日 24 城汇总表（含发报时间），筛选条件全部可用，结果与 CLI 版一致。

## 阶段二：城市动态管理（需求 1）

**内容**
- 城市选择面板：**一级目录为省/直辖市**，展开后为该省全部站点，支持勾选、全选、搜索过滤（按城市名/拼音首字母可选）
- 选中列表存 `localStorage`，刷新不丢；"恢复默认"回到内置列表
- **导出/导入**：一键复制当前城市列表为文本（每行一个城市名），与 `cities.txt` 格式互通；也支持粘贴文本批量导入
- 未收录城市的校验复用阶段一的报错链路

**验收**：勾选变更即时反映到汇总表；导出文本可直接粘贴进 `cities.txt` 被 Python 脚本使用。

## 阶段三：周末/节假日自动识别（需求 2 的一部分）

**内容**
- 日期快捷按钮：**本周末 / 下周末 / 下个法定节假日**（自动跳过已过去的假期），点击自动填充日期范围
- 新增 `holidays.json`：法定节假日与调休安排，结构如 `{"2026-10-01": "国庆节", ...}`，每年国务院发布后手工更新一次
- 数据过期降级：`holidays.json` 覆盖不到当前日期时，仅提供周末快捷按钮并提示"假期数据待更新"，不影响其他功能
- 周四/周五打开页面时默认选中"本周末"

**验收**：任意日期打开，"本周末"按钮正确填入周六~周日；假期数据覆盖期内"下个假期"正确（含调休拼接）。

## 阶段四：结果分享（需求 7）

**内容**
- 将当前状态（选中城市、日期范围、全部筛选条件）编码进 URL hash，如 `#cities=上海,杭州&from=08-15&to=08-16&rain=2`
- "复制分享链接"按钮；他人打开链接即还原相同视图并自动抓取最新数据
- "复制文本汇总"：生成与 CLI 输出同格式的纯文本表格，方便发微信/邮件

**验收**：分享链接在无痕窗口打开后视图完全一致；文本汇总可直接粘贴阅读。

## 阶段五：预报变更对比（需求：周四看一遍、周五再看一遍）

典型场景：周四筛好目的地，周五确认时最关心"天气有没有变坏"。

**内容**
- 每次成功抓取后，将本次结果快照存入 `localStorage`（城市 × 日期 → 天气文字、雨况档位、昼夜温度、降水量），保留上一次快照
- 加载时新旧快照对比，页面顶部展示**变更清单**，按"变坏程度"排序：
  - 雨况档位上升：`杭州 周日 晴→中雨 ⚠️`（最严重，置顶）
  - 温度显著变化（阈值暂定 ≥3°C）：`苏州 周六 高温 31→35°C`
  - 变好不强调，用浅色或折叠（`南京 周日 小雨→晴 ✓`）
  - 新增/移除的城市、消失的日期也列出
- 汇总表对应单元格同时高亮（变坏红底、变好绿底），一眼定位
- 快照超过 24 小时的，变更清单顶部标注"对比基准为 X 月 X 日 HH:MM 的数据"
- 提供"清除对比基准"按钮

**验收**：周四抓取后修改本地快照模拟变化，周五重新打开页面，变更清单正确显示"晴→中雨"类条目并高亮对应行。

## 阶段六：出行数据（需求 3）

**内容**
- 城市元数据结构预留：每城市可填 `高铁 / 飞机 / 自驾` 三种方式的时长（小时，可留空），存储于 `localStorage`，支持导出/导入 JSON
- 汇总表增加"出行时长"列；筛选增加"X 小时圈内"条件（任一方式 ≤ X 即入选）
- 数据靠用户手工录入，页面提供编辑入口；不自动抓取第三方耗时数据

**验收**：录入安吉高铁 1.5h、自驾 3h 后，"2 小时圈内（高铁）"筛选只命中符合条件的城市。

## 阶段七：气象预警接入（需求 4）

**内容**
- 抓取 `/rest/findAlarm?province={省}`（已验证 CORS 可用），按选中城市涉及的省份去重查询
- 用城市名匹配预警 `title`（如"杭州市气象台发布台风黄色预警"），命中即在汇总表该城市行标注预警徽标，按蓝/黄/橙/红分级着色
- 点击徽标展开预警详情列表，链接到 NMC 原文页
- `/rest/weather` 响应里的 `data.real.warn` 可作为单城市预警的快速来源，与 findAlarm 互补
- 预警数据失败时不影响主流程，仅隐藏徽标

**验收**：有台风/暴雨预警时对应城市行出现分级徽标；无预警省份不产生多余请求开销。

## 阶段八：温度走势可视化（需求 5）

**内容**
- 多选若干城市，在同一折线图叠加显示：每城市两条线（高温、低温），颜色区分城市
- **数据源**（比原计划简化）：`/rest/weather` 响应自带 `data.tempchart`（前 7 天实况 + 后 7 天预报），一次请求全部拿到，纯前端即可画完整 14 天走势，无需等 Actions 兜底
- 图上以竖线区分"今天"：左侧实况、右侧预报
- 图表库：Chart.js（CDN 引入）或手写 SVG，优先零构建复杂度低的方案

**验收**：勾选 3 个城市后图表正确叠加 6 条线（14 天），图例、温度轴、日期轴、今/预报分界清晰。

## 阶段九（可选）：Actions 定时兜底

**内容**
- GitHub Actions 每天 07:00 / 13:00 / 19:00（对应 NMC 三次发报）运行 `nmc_weather.py`，产出 `data.json` 提交回仓库
- 前端直连 NMC 失败时自动回退读 `data.json`，并标注"数据快照，更新于 HH:MM"
- 解决：NMC 改 CORS 策略、弱网环境
- 注意：阶段八之后 tempchart 已不再依赖此机制，本阶段纯属保险

**验收**：停用直连后页面仍显示快照数据与时间标注。

---

## 实施顺序与依赖

```
阶段一 (MVP) ──► 阶段二 (城市管理) ──► 阶段三 (周末节假日)
       │                │
       │                └──► 阶段四 (分享，依赖城市/条件状态)
       │
       └──► 阶段五 (变更对比，仅需阶段一 + localStorage)
       │
       └──► 阶段六 (出行数据) ──► 阶段七 (预警) ──► 阶段八 (温度图)
                                                        ▲
                                          阶段九 (Actions 兜底，纯保险，可穿插)
```

- 阶段一是所有功能的地基，优先完成并部署，后续每阶段独立可上
- 阶段二、三、五直接服务"周四初筛、周五确认"的真实使用流程，建议紧随其后
- 阶段八不再依赖阶段九（tempchart 已确认在 `/rest/weather` 响应内）
- 每阶段完成后更新 `README.md` 的用法说明
